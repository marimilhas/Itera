const urlServidor = "http://localhost:3000"
const urlResourceProyectos = urlServidor + "/api/proyectos";
const urlResourceCiclos = urlServidor + "/api/ciclos";
const urlResourceLogin = urlServidor + "/api/login";

export const config = {
    urlServidor,
    urlResourceProyectos,
    urlResourceCiclos,
    urlResourceLogin
}
